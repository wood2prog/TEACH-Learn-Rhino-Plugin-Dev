# Lesson 0002 completed: Custom docking panels

Lesson 0002 covered custom UI panels in RhinoCommon: registering a UserControl via `Panels.RegisterPanel`, open/close commands, and document event watchers (`AddRhinoObject`, `DeleteRhinoObject`, etc.) with cross-thread UI update handling via `InvokeRequired`.

The user is now positioned for Lesson 0003 (Plugin UserData — persisting data in `.3dm` files), which connects their UI knowledge to data persistence.
